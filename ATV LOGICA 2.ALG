Algoritmo "atv logica 2"
// Disciplina   : [Linguagem e L�gica de Programa��o]
// Professor   : Antonio Carlos Nicolodi
// Descri��o   : Aqui voc� descreve o que o programa faz! (fun��o)
// Autor(a)    : Nome do(a) aluno(a)
// Data atual  : 21/09/2023
Var
   // Se��o de Declara��es das vari�veis
   nome:caractere
   n1, n2 ,n3, mediaFinal :real
   
Inicio
  funcao calcularMedia(n1, n2, n3: real): real
     var
      meidafinal: real 
      inicio 
       media <- (n1 * 2 + n2 * 3 + n3 * 5) / 10
       retone mediaFinal
  fimFunacao 
   // Se��o de Comandos, procedimento, fun��es, operadores, etc...
   //escreval("nome do aluno?")
   //leia(nome)
   //escreval("nota no primeiro semestre")
   //leia(n1)
   //escreval("nota no semestre semestre")
   //leia(n2)
   //escreval("nota no terceiro semestre")
   //leia(n3)

   escreval("sua media final é:",mediaFinal)

 calcularmedia(10,4,8.1)

Fimalgoritmo